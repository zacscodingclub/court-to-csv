require "spec_helper"

describe Court::To::Csv do
  it "has a version number" do
    expect(Court::To::Csv::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
